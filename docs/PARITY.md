# Parity tracker

One row per screen, notable state, or feature. Status: `todo` · `in-progress` · `done` · `blocked`. Screenshots = raw `adb` PNGs in `App Screenshots/` (1080×2340).

## Foundation (Phase 1)

| Item | Status | Notes |
|---|---|---|
| Vite + React + TS scaffold | done | Node 24, React 19, TS 6, strict + noUncheckedIndexedAccess |
| Folder structure | done | app/ features/ ui/ hooks/ data/ theme/ |
| CSS tokens | done | `npm run tokens` → tokens.css; 4 themes, all sizes |
| Fonts (WOFF2) | done | Noto Naskh Arabic + Noto Sans Myanmar, offline-bundled |
| sync-assets script | done | Copies 235 CSVs + downloads fonts |
| CSV parsers + unit tests | done | 11 tests pass; handles 217a/217b, missing 171, CRLF, LF |
| Visual check script | done | pixelmatch, 1% limit; identical→PASS, shifted→FAIL verified |
| PWA setup | done | vite-plugin-pwa, manifest, icons; CSV set fetched by dataCache.ts (visible splash progress) instead of silent precache, still served via runtime CacheFirst route. 2026-09-23: `short_name` is `Quran (KW)` — launchers, not the manifest, decide how many lines an icon label gets (Pixel/Launcher3 ellipsize on one line, One UI and Nova wrap to two), so the label is kept short enough to fit one line uncut everywhere. `name` and `<title>` keep the full `Quran MM (KW)` for the install prompt, app switcher and browser tab. An installed icon keeps its old label until the WebAPK updates (reinstall to see it at once; on iOS delete and re-add) |
| App shell + routes | done | BrowserRouter; `/` reader, `/s/:surah/:ayat`, `/info/:page` |
| Storage persist | done | `navigator.storage.persist()` on startup |

| Item | Type | Screenshot | Route | Status | Notes |
|---|---|---|---|---|---|
| Splash | screen | 11.png | `/` | done | kw_logo centered, near-white bg, 2000 ms; visual test uses 11-chrome.png (Android ICC color diff). 2026-09-18: shows a progress bar ("Preparing offline data N / total") on first launch only, while dataCache.ts fetches the CSV set — deviation from Android, not in a screenshot. The warm-up runs only for an installed launch (display-mode standalone, or navigator.standalone on iOS); a browser tab goes straight to the reader and relies on the service worker's per-surah CSV caching. KNOWN: `npm run test:visual -- splash` fails at 5.77% (logo offset/scale against 11-chrome.png), predates the progress bar — untriaged |
| Reader (single-column) | screen | 01.png (Blue) | `/` | done | HorizontalPager, 3 cards per ayat page; Chrome baseline 01-chrome.png. Owner polish 2026-09-18: Surah/Ayat chips use `--shadow-chip` (4-sided elevation, was bottom-only) and `--chip-text-nudge` (optical centring against Noto Sans Myanmar's lopsided line-box metrics); Myanmar surah name uses `--lh-myanmar: 1.7` so stacked diacritics are not clipped — all intentional deviations from Android. 2026-09-20: `#` in the CSVs is the app's line-break marker (3008 of them, 2688 in `*_notes.csv`; `# #` = blank line, 107 occurrences). Android replaces it in every text path (MainActivity.kt:1611, :1613, :1623); the PWA only did so for the main translation, so the Tafsir card and the `@` translation note rendered raw `#`. Both now go through the same replace, and the translation note renders via `TranslationText` like Android's JumpableText, so it gets footnote and cross-ref styling too. 2026-09-22: Surah/Ayat chips rebuilt to Android's own layout — one two-line `Text` (label above, numbers below), bold, `theme.primary`, on `theme.bg` (same colour as the page) with Material 4 dp elevation plus a 1 dp pressed state, so they read as buttons. The white background and the `--chip-text-nudge` optical centring are gone; `Ayat` now formats as `1 / 110` (spaces), `Surah` as `18/114`, matching MainActivity.kt. Chip geometry measured against 01.png: ours 273-436 x 19-140 px vs Android 275-434 x 22-135 px. 2026-09-22: combined ayats now display as a range — the Ayat chip and the Arabic card title show the raw `multi_ayats` cell (`1-2`), falling back to `Ayat_id` when it is blank, matching MainActivity.kt:1492 and :1715; `ayatLabel`/`parseAyatIds`/`ayatIdsOf` live in `data/csv.ts` so paging, jump-to-ayat and Arabic assembly all share one definition. `npm run test:visual -- reader` is 5.30% because its baseline `01-chrome.png` is a stale Chrome capture — it already failed at 4.98% before this change (content bands, from the earlier `#` line-break fix); baseline not re-saved. 2026-09-23: the Arabic card's PlaylistPlay button now ports `isPlayForwardEnabled = loadedAyatIndex == page` (MainActivity.kt:1675) — it had been hard-wired enabled (`audioLoaded={true}`). `useAudio` tracks `loadedAyat`: set by the card's own play button (:784), kept through a pause, cleared when that track ends (:326-329), when "play surah from here" fires (:1614) and on surah change (:663). Disabled tint is now Compose `Color.LightGray` at 50% alpha (new `--color-icon-disabled` token), not a faded primary (:1899). Covered by `src/hooks/useAudio.test.ts`; visual diff 5.30% → 5.28% against the same stale baseline. KNOWN deviation: the card's play button restarts the ayat instead of pausing/resuming like Android's `toggleAudio`, so the Pause icon it shows is misleading — untriaged. 2026-09-23: audio auto-tracking used to cut straight to the next page through `goTo`, which gave no sign the page had turned. When the target is the very next page of the same surah and the pager is idle, the track now runs the same forward slide a hand swipe ends with, at 500 ms `ease-in-out` instead of the 280 ms snap (no finger on the screen, so the motion has to read on its own). Everything else — surah change, a multi-page jump, a move arriving mid-drag — still jumps. A `transitionend` that never arrives (hidden tab, cancelled transition) is covered by a 620 ms fallback timer, so playback cannot outrun the page, and `transitionend` now ignores events bubbling up from the cards |
| Reader — Pink theme | state | 12.png | `/` | n/a | Descoped by owner 2026-09-17 |
| Reader — empty Tafsir notes | state | 12.png | `/` | n/a | Descoped by owner 2026-09-17 |
| Feature: Swipe paging (L/R between ayats) | feature | — | `/` | done | Touch-driven 3-slot pager in ReaderScreen; threshold 60px; snap animation 0.28s |
| Reader — history icon active | state | — | `/` | done | 2026-09-22: tint was already `--color-primary` when `jumpHistory` is non-empty; the button is now also `disabled` while the history is empty, matching `clickable(enabled = hasHistory)` (BlueprintTopBar.kt:58). Verified in Chrome; no Android baseline captured |
| Nav drawer | drawer | 02.png | overlay | done | 320 dp; Audio / App Settings / Translator's Info + Developer/About/Exit; switches toggle prefs; switch track = green (Material default); Chrome baseline 02-chrome.png; status bar in raw adb screenshots = 100px (38dp at 420dpi) |
| Info viewer — Preface | screen | 04.png | `/info/preface` | done | WebView → inline HTML; Chrome baseline 04-chrome.png; visual 0.00% PASS |
| Info viewer — Introduction | screen | 05.png | `/info/introduction` | done | Chrome baseline 05-chrome.png; visual 0.00% PASS |
| Info viewer — Biography | screen | 06.png | `/info/biography` | done | Chrome baseline 06-chrome.png; visual 0.00% PASS |
| Info viewer — Developer | screen | 07.png | `/info/developer` | done | Title "Developer" (was "Developer / About"); 3 title+background sections (Intention / Disclaimer of Liability / ရှင်းလင်းချက်) mirrored from developer.html's own CSS; Chrome baseline 07-chrome.png; visual 0.00% PASS |
| Dialog: Select Surah | dialog | — | overlay | done | Scrollable list, current surah highlighted + auto-scrolled; tap row → opens Jump to Ayat; same iOS Safari `flex: 1` list-collapse bug fixed (see Select Surahs to Download row), plus WebKit ignores `scrollIntoView` inside a dialog opened in the same frame — auto-scroll now sets `scrollTop` in a `requestAnimationFrame`. No visual baseline exists for this dialog |
| Dialog: Jump to Ayat | dialog | — | overlay | done | Outlined input, validates 1..totalAyats, Go = filled primary pill; opened from Ayat chip or after surah select |
| Dialog: Select App Theme | dialog | 03.png | overlay | done | Blue / Green / Pink / Brown; Chrome baseline 13-chrome.png; visual check PASS 0.00% |
| Dialog: Select Surahs to Download | dialog | 09.png | overlay | done | Chrome baseline 09-chrome.png; visual 0.00% PASS; download mechanics live against the ngrok HTTPS host; fixed iOS Safari bug where surah list rendered 0-height (`.list { flex: 1 }` in an auto-height dialog collapses under WebKit; use `flex: 0 1 auto; min-height: 0`). 2026-09-26: default state (all checked) and the Surah 1 lock were missing — Surah 1's checkbox is now always disabled+checked and "Uncheck All" clears everything else but leaves it checked, per MainActivity.kt:890 and user screenshot. |
| Dialog: Download progress | dialog | — | overlay | done | "Downloading Audio" modal: dual bars, ETA (mm:ss), Pause/Resume + Stop; sequential engine, resumable via cache-skip; built to spec + image #4; component test green. No adb capture → no pixel visual test (like other capture-less dialogs). Live download works over the ngrok HTTPS host; ngrok free returns its ERR_NGROK_6024 interstitial (no CORS headers) to cors fetch(), so `config.ts` adds `ngrok-skip-browser-warning` for ngrok hosts only. 2026-09-18: the loop runs the surah's real ayat ids, not 1..numberOfAyahs — surah 1 starts at 0 (basmala, 001000.mp3), so its first page downloaded nothing and stalled offline while a nonexistent 001007.mp3 was fetched instead. |
| Dialog: Select Audio to Delete | dialog | 10.png | overlay | done | Cache API entries, not device files; title/freed-size hardcoded #D81B60; Chrome baseline audio-delete-chrome.png; visual 0.00% PASS; same iOS Safari `flex: 1` list-collapse bug fixed (see Select Surahs to Download row). 2026-09-26: "Check All" was excluding Surah 1 entirely (its checkbox stays individually disabled, but Check All must still sweep it into the selection, gray + deletable); fixed so `allSelected`/`toggleAll` cover the full cached list, not just the unlocked ones. |
| Dialog: Delete progress | dialog | — | overlay | todo | Single progress bar (needs capture) |
| Dialog: Jump History | dialog | — | overlay | done | 2026-09-22: built as `features/dialog-jump-history/`. Opens from the History icon when the session holds more than 2 steps; rows are `Original Reading Spot` / `Jump Step N` / `Current Spot` over `\u202A[Surah S : Ayat A]\u202C EnglishName`, 72 px M3 two-line rows on the theme background, dividers `rgba(204,204,204,0.5)`, footer `Clear History` / `Close`. Row tap truncates the stack to that step (step 0 ends the session); measured against the owner's Android capture: dialog 329 px wide vs 332, rows 72 px, text insets 24/40 px. No Android baseline in `App Screenshots/`, so there is no `test:visual` id for it — verified in Chrome at 411×891 |
| Dialog: About App | dialog | 08.png | overlay | done | Rebuilt to the measured Android values: card 320dp/16dp radius, title 24sp, body 14sp/20sp on #49454F, disclaimer 12sp/16sp #888888, **one** divider (was four), 24dp + 28dp section gaps. Baseline renamed dialog-about-chrome.png (08-chrome.png is the stale pre-fix one); visual 0.00% PASS. Every band within 1dp of 08.png except the title (3.4dp high) — Roboto not self-hosted yet |
| Dialog: Update Available | dialog | — | overlay | n/a | DROPPED — Android APK updater, not for PWA |
| Feature: Theme system (4) | feature | 03.png | — | done | `app_theme` pref; SelectThemeDialog wired to onPrefsUpdate |
| Feature: Font scaling A-/A+ | feature | 01.png | — | done | 3 independent scales in AppPrefs; persisted to localStorage on each tap |
| Feature: Footnote markers + jump links | feature | 01.png | — | done | pink superscripts (`--color-accent`) + `[surah:ayat]` tappable links; `TranslationText` + `NotesText` in RichText.tsx |
| Feature: Jump history session | feature | — | — | done | 2026-09-22: `useSurah` ports `isHistoryActive` + `jumpHistory` (MainActivity.kt:397-428). Only a `[surah:ayat]` link starts a session (`jumpFromLink`), seeding step 0 with the pre-jump spot; while active, every page the reader settles on for 500 ms becomes a step, a revisited position moves to the end, and settling back on step 0 ends the session and wipes the history. The keypad Go and surah selection never start a session. In-memory only, like Android. Deviation: `JumpStep` is `{surahId, ayatId}` — Android also restores the card list's scroll offset on return, the PWA lands at the page top |
| Feature: Last-read position | feature | — | — | done | `scheduleSave` called on all navigation (prevPage/nextPage/prevSurah/nextSurah/goTo); 500ms debounce; gated by `rememberLastRead` pref |
| Feature: Continuous swiping | feature | — | — | done | prevPage/nextPage wrap to prev/next surah at boundaries; NavDrawer switch wired |
| Feature: Audio playback | feature | — | — | done | `useAudio` hook; single-ayat + surah-sequential play; auto-track; `config.ts` single-source URL. Playback fetches the ayat and plays a blob URL instead of `el.src = <remote>` — a media element cannot send `ngrok-skip-browser-warning`, so ngrok's interstitial returned HTML it could not decode. Downloaded ayats come from Cache Storage; undownloaded ones stream and are not cached, so the Delete Audio list still means "downloaded", like native. 2026-09-18: cache entries are keyed by path (`/audio/001/001001.mp3`), not by the audio host — a host-keyed entry from an earlier build was invisible to the lookup, so an ayat listed as downloaded silently re-streamed and failed in airplane mode; a one-time key sweep recovers those older entries. 2026-09-23: the top bar's play button now ports Android's playlist head (MainActivity.kt:820-855). It had resumed whatever was last loaded and otherwise started at the open page's ayat; it now starts at `001/001000.mp3` (the Bismillah, always folder 001) and runs on into the surah's own first ayat, skipping the prepend for surah 1 (its ayat 0 *is* that file) and surah 9 (no Bismillah). The highlight sits on ayat 1 for the whole Bismillah track, as in Android where both map to the same page (:834 vs :842). Only a live playlist for the current surah resumes; a finished playlist, a single-ayat track or a surah change rebuilds from the head, matching Android's empty/idle `surahPlayer` check and its `STATE_ENDED` `seekTo(0, 0)` (:864-866). The card's "play surah from here" gets the same head when it starts at ayat 1, which Android reaches through the Bismillah sharing ayat 1's page in `surahAudioTrackToPageMap` (:1663, :1700-1701). KNOWN deviation: Android rewinds up to 1.5 s on resume (:867-870); the PWA resumes at the exact paused position — untriaged. |
| Feature: Update checker | feature | — | — | n/a | DROPPED — Android-only |
| State: Dark mode | state | — | — | n/a | App is light-only; no dark theme exists |
